### File managed with puppet ###
### Served by:        '<%= scope.lookupvar('::servername') %>'
### Module:           '<%= scope.to_hash['module_name'] %>'

<LoadPlugin exec>
    Globals true
</LoadPlugin>

<Plugin exec>
    Exec "nobody" "/usr/local/collectd-plugins/iostat.rb"
</Plugin>